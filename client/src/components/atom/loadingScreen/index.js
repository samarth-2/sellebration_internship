import { DotWave } from '@uiball/loaders'

const LoadingScreen = (props) => {
  return(

    <DotWave
     size={60}
     lineWeight={5}
     speed={1.4} 
     color="black" 
    />
  );
};
 
export default LoadingScreen;